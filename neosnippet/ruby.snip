snippet proxy
abbr for Vagrantfile
  if Vagrant.has_plugin?("vagrant-proxyconf") && ENV['HTTP_PROXY']
    puts '# - Proxy Setting ----------------------------------'
    puts '# ' + ENV['HTTP_PROXY']
    config.proxy.http     = ENV['HTTP_PROXY']
    config.proxy.https    = ENV['HTTP_PROXY']
    config.proxy.no_proxy = "localhost,127.0.0.1"
    puts '# --------------------------------------------------'
  end
